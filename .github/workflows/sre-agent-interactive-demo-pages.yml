name: Publish SRE Agent interactive demo

on:
  push:
    branches:
      - main
    paths:
      - "sre-agent-interactive-demo/**"
      - ".github/workflows/sre-agent-interactive-demo-pages.yml"
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Configure GitHub Pages
        uses: actions/configure-pages@v5

      - name: Upload interactive demo
        uses: actions/upload-pages-artifact@v3
        with:
          path: sre-agent-interactive-demo

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
